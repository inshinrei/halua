import { describe, test } from "vitest"

// are these needed>
describe("Halua logger e2e usage", () => {
    test.todo("create new instance for logging")

    test.todo("create multiple instances")

    test.todo("append args with With method")
})
