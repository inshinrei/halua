decision record

### 25.1-next

- `messageFormat` should be removed, may be hard to maintain, and it's seems to be overheaded
- `pretty` option should be removed from `WebConsoleHandler`, instead `PrettyConsoleHandler` should be implemented